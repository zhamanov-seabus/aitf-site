# NAU AI Task Force site (static)

Static copy of the Task Force site, served by GitHub Pages at https://aitf.na.edu.
The NAUmi assistant is not hosted here: `index.html` calls it cross-origin (see `API_BASE`).
Source of truth: `clients/nau_ai_taskforce_site/` in the maintainer's workspace; copy `index.html` and `images/` here and push.
